name = "boltpay-og-preview"
main = "og-preview-worker.js"
compatibility_date = "2025-01-01"

[[routes]]
pattern = "pay.parvez.website/u/*"
zone_name = "pay.parvez.website"

# Note: if you deploy via the Cloudflare dashboard (Quick Edit) instead
# of wrangler CLI, add this route manually under
# Worker → Settings → Triggers → Add route, using the same pattern/zone.
